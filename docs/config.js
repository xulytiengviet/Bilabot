/* Set these PUBLIC identifiers after deploying your own gateway.
   No secrets belong in GitHub Pages. Users can also supply the same
   non-secret values in the advanced configuration panel locally. */
window.BILABOT_CONFIG = Object.freeze({
  workerUrl: '',
  googleClientId: '',
  autoPair: true
});
