name = "bilabot-gateway"
main = "src/index.js"
compatibility_date = "2026-07-14"
workers_dev = true

[vars]
PAGE_ORIGIN = "https://xulytiengviet.github.io"
TURNSTILE_SITE_KEY = ""
# Configure public TURNSTILE_SITE_KEY above; add secret TURNSTILE_SECRET via wrangler.
# Add SESSION_SECRET (>=32 chars) and TICKET_KEY (64 hex chars) via wrangler secret put.
# No Google OAuth credentials are needed.
